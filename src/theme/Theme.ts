export type SolidDebuggerTheme = {
  colors: {
    backgroundColor: string;
    foregroundColor: string;
    borderColor: string;
    cursorColor: string;
    cursorAccentColor: string;
    selectionColor: string;
    ansi: {
      black: string;
      red: string;
      green: string;
      yellow: string;
      blue: string;
      magenta: string;
      cyan: string;
      white: string;
      lightBlack: string;
      lightRed: string;
      lightGreen: string;
      lightYellow: string;
      lightBlue: string;
      lightMagenta: string;
      lightCyan: string;
      lightWhite: string;
    };
  };
};
